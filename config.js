// Fill these in with your own Supabase project's values to connect a real,
// shared backend (see README.md "Setup" for step-by-step instructions).
//
// It is safe to commit real values here: the anon/public key is designed
// to be exposed in client-side code — access control is enforced by the
// Row Level Security policies in schema.sql, not by keeping this key secret.
//
// Until both values below are filled in, the app runs in DEMO MODE: sample
// events and a local-only account, stored just in your browser, so you can
// try the whole UI before setting up a backend.
window.APP_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
};
